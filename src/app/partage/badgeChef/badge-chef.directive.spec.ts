import { BadgeChefDirective } from './badge-chef.directive';

describe('BadgeChefDirective', () => {
  it('should create an instance', () => {
    const directive = new BadgeChefDirective();
    expect(directive).toBeTruthy();
  });
});
